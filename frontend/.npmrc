# Angular CLI resolves many packages via the flat node_modules layout that npm
# produces by default. With pnpm's strict hoisting, some tools fail to find
# peer dependencies unless we hoist everything to the root.
shamefully-hoist=true
strict-peer-dependencies=false
